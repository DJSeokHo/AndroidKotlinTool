<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#000000</item>

        <item name="toolbarNavigationButtonStyle">@style/ToolBarNavigationIconStyle</item>
        <item name="actionOverflowButtonStyle">@style/ToolBarOverlayIcon</item>


        <item name="android:windowSharedElementEnterTransition">@transition/detail_scene_transition</item>
        <item name="android:windowSharedElementExitTransition">@transition/detail_scene_transition</item>

    </style>

    <style name="ToolBarNavigationIconStyle" parent="@style/Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>
    <style name="ToolBarOverlayIcon" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:tint">#ffffff</item>
    </style>

    <style name="BottomSheetDialogBg" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
